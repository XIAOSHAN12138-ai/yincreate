# 视频生成功能更新 - 后端接口更新说明

## 更新日期
2026-07-02

## 更新内容
1. **声音控制**：视频生成新增有声/无声开关
2. **双上传框**：特定特色功能支持双上传框（首帧+尾帧 / 主体+参考图）
3. **管理员模型配置**：管理员可通过 UI 配置模型的声音模式、最大时长、分辨率、特色功能、可见性

---

## 一、声音控制功能

### 按钮状态规则

| 模型 | 按钮状态 | 默认值 | 可否切换 |
|------|----------|--------|----------|
| GV3.1 | 可交互 | 保留用户上次选择 | ✅ 可自由切换 |
| Kling 2.6 | 可交互 | 保留用户上次选择 | ✅ 可自由切换 |
| Kling 3.0 | 可交互 | 保留用户上次选择 | ✅ 可自由切换 |
| Kling 3.0-Omni | 可交互 | 保留用户上次选择 | ✅ 可自由切换 |
| HappyHorse 系列 | 禁用（灰色） | 有声（`true`） | ❌ 不可切换 |
| 其他模型 | 禁用（灰色） | 无声（`false`） | ❌ 不可切换 |

### 参数说明

| 参数名 | 类型 | 位置 | 可选值 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|--------|------|------|
| `sound` | boolean | `parameters.sound` | `true`, `false` | `false` | 否 | 是否生成有声视频 |

---

## 二、双上传框功能

### 功能概述
当用户选择特定视频特色功能时，输入框区域会出现两个专用上传框，替代通用的上传按钮，引导用户上传特定用途的素材。

### 支持双上传框的特色功能

| 特色功能 ID | 特色功能名称 | 上传框1 (slot1) | 上传框2 (slot2) |
|-------------|-------------|-----------------|-----------------|
| `first-last-frame` | 首尾帧 | 首帧 (first_frame) | 尾帧 (last_frame) |
| `ai-outfit` | AI换装 | 主体 (subject) | 参考图 (reference) |
| `scene-replace` | 场景替换 | 主体 (subject) | 参考图 (reference) |
| `style-replace` | 风格替换 | 主体 (subject) | 参考图 (reference) |
| `effect-copy` | 特效复刻 | 主体 (subject) | 参考图 (reference) |
| `motion-imitate` | 动作模仿 | 主体 (subject) | 参考视频 (reference) |

### input_files 中 purpose 字段说明

双上传框模式下，`input_files` 中每个文件的 `purpose` 字段不再是通用的 `reference`，而是使用具体的用途标识：

| purpose 值 | 含义 | 对应特色功能 |
|-----------|------|-------------|
| `first_frame` | 首帧图片 | 首尾帧 |
| `last_frame` | 尾帧图片 | 首尾帧 |
| `subject` | 主体素材 | AI换装、场景替换、风格替换、特效复刻、动作模仿 |
| `reference` | 参考素材 | AI换装、场景替换、风格替换、特效复刻、动作模仿 |

---

## 请求示例

### 首尾帧功能

```json
{
    "output_type": "video",
    "model": "kling_3_0",
    "vendor": "vendor_a",
    "feature": "first-last-frame",
    "parameters": {
        "resolution": "1080P",
        "duration": 5,
        "ratio": "16:9",
        "sound": false
    },
    "prompt": "从首帧过渡到尾帧",
    "input_files": [
        {
            "type": "image",
            "url": "data:image/jpeg;base64,...",
            "purpose": "first_frame",
            "object_id": "first_frame_1"
        },
        {
            "type": "image",
            "url": "data:image/jpeg;base64,...",
            "purpose": "last_frame",
            "object_id": "last_frame_1"
        }
    ]
}
```

### AI换装功能

```json
{
    "output_type": "video",
    "model": "kling_3_0",
    "vendor": "vendor_a",
    "feature": "ai-outfit",
    "parameters": {
        "resolution": "1080P",
        "duration": 5,
        "ratio": "16:9",
        "sound": false
    },
    "prompt": "换上参考图中的服装",
    "input_files": [
        {
            "type": "image",
            "url": "data:image/jpeg;base64,...",
            "purpose": "subject",
            "object_id": "subject_1"
        },
        {
            "type": "image",
            "url": "data:image/jpeg;base64,...",
            "purpose": "reference",
            "object_id": "reference_1"
        }
    ]
}
```

### 动作模仿功能（主体图片+参考视频）

```json
{
    "output_type": "video",
    "model": "kling_3_0",
    "vendor": "vendor_a",
    "feature": "motion-imitate",
    "parameters": {
        "resolution": "1080P",
        "duration": 5,
        "ratio": "16:9",
        "sound": false
    },
    "prompt": "让主体模仿参考视频的动作",
    "input_files": [
        {
            "type": "image",
            "url": "data:image/jpeg;base64,...",
            "purpose": "subject",
            "object_id": "subject_1"
        },
        {
            "type": "video",
            "url": "data:video/mp4;base64,...",
            "purpose": "reference",
            "object_id": "reference_1"
        }
    ]
}
```

### 普通视频生成（无双上传框）

```json
{
    "output_type": "video",
    "model": "Seedance 2.0 Fast",
    "vendor": "vendor_a",
    "feature": "text_to_video",
    "parameters": {
        "resolution": "1080P",
        "duration": 5,
        "ratio": "16:9",
        "sound": false
    },
    "prompt": "一只猫在追蝴蝶",
    "input_files": []
}
```

---

## 后端处理建议

1. **purpose 字段识别**: 根据 `input_files[].purpose` 判断文件用途
   - `first_frame` / `last_frame`: 首尾帧功能的首帧和尾帧
   - `subject`: 主体素材（人物、物品等）
   - `reference`: 参考素材（服装、场景、动作视频等）
   - 通用 `reference`（非双上传框模式）: 保持原有逻辑

2. **向下兼容**: 
   - `sound` 字段缺失时默认 `false`
   - `purpose` 字段缺失时默认 `reference`
   - 非双上传框功能不受影响

3. **文件校验**: 
   - `first_frame` 和 `last_frame` 必须为图片
   - `subject` 通常为图片
   - `reference` 可为图片或视频（取决于特色功能）

---

## 测试用例

### 声音控制

| 场景 | 模型 | 按钮状态 | sound 值 | 预期结果 |
|------|------|----------|----------|----------|
| 自由选择 + 默认 | Kling 3.0 | 可交互 | `false` | 生成无声视频 |
| 自由选择 + 切换有声 | Kling 3.0 | 可交互 | `true` | 生成有声视频 |
| 强制有声 | HappyHorse | 禁用 | `true` | 生成有声视频 |
| 强制无声 | Seedance 2.0 | 禁用 | `false` | 生成无声视频 |

### 双上传框

| 场景 | 特色功能 | 上传框1 | 上传框2 | purpose 值 |
|------|----------|---------|---------|-----------|
| 首尾帧 | first-last-frame | 首帧 | 尾帧 | first_frame, last_frame |
| AI换装 | ai-outfit | 主体 | 参考图 | subject, reference |
| 场景替换 | scene-replace | 主体 | 参考图 | subject, reference |
| 风格替换 | style-replace | 主体 | 参考图 | subject, reference |
| 特效复刻 | effect-copy | 主体 | 参考图 | subject, reference |
| 动作模仿 | motion-imitate | 主体 | 参考视频 | subject, reference |
| 全能参考 | all-reference | 无双上传框 | - | reference（通用） |

---

## 相关文件

- [GenerateView.vue](src/views/GenerateView.vue) - 前端页面组件
- [前端API接口文档.md](前端API接口文档.md) - 完整 API 文档
