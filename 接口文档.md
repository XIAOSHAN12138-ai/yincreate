# AgentScope 多轮对话服务开发接入文档

适用版本：服务 `0.1.0`，AgentScope `2.0.5`

Python、环境变量、源码交付和生产部署见 [环境配置文档.md](环境配置文档.md)。

## 先看这里

推荐直接使用**平台模式 `/api/*`**：服务端自带 SQLite，自动保存会话状态、消息与长期记忆。客户端只需创建会话并传消息，**无需管理任何状态**。

平台模式三步接入：

1. `POST /api/conversations` 创建会话，得到 `conversation_id`。
2. 对会话调 `POST /api/conversations/{id}/chat`（或 `/chat/stream`）发送消息。
3. 服务端已自动持久化；可用 `GET /api/conversations/{id}` 查看历史消息与状态。服务重启后不带状态继续发消息即可续聊。

> 进阶：若客户需要完全自建前端/数据库并掌控状态，可改用无状态 `/v1/*` 接口（见第 2 节）。服务内部仍使用 AgentScope 的 `Agent`、`AgentState`、`ChatModelBase` 等组件扩展模型与对话流程。

---

## 1. 平台模式（默认，服务端自动持久化）

推荐直接使用平台模式：服务端自带 SQLite（`data/platform.sqlite3`），自动保存会话状态、消息与长期记忆。客户端只需创建会话并发送消息，**全程不需要管理 `conversation_state` / `user_memory_state`**。

### 建会话

```http
POST /api/conversations
Content-Type: application/json

{
  "tenant_id": "tenant-a",
  "user_id": "user-123",
  "agent_id": "deepseek-text",
  "memory_enabled": false
}
```

响应 201，返回会话对象（含 `id`，即 `conversation_id`）。

### 发起对话（非流式）

```http
POST /api/conversations/<conversation_id>/chat
Content-Type: application/json

{
  "tenant_id": "tenant-a",
  "user_id": "user-123",
  "message": {
    "content": [{"type": "text", "text": "我叫张三"}]
  }
}
```

响应字段与无状态 `/v1/chat` 相同（`assistant_message`、`conversation_state`、`new_state_version`、`usage` 等）。服务端会在同一个事务里把本轮消息、会话状态和长期记忆（如有）写入 SQLite。

### 流式对话

`POST /api/conversations/<conversation_id>/chat/stream`，请求体与上面相同。事件顺序为 `started` → `delta*` → `completed`（或 `error`）。只有 `completed` 事件会落库；`error`、断流不改变旧状态。

### 重启后继续

服务重启后，客户端不带任何状态、用同一 `conversation_id` 继续发消息即可，服务端会自动从 SQLite 恢复上下文。

### 长期记忆

- `GET /api/memories`：查看某租户+用户+Agent 的长期记忆。
- `POST /api/memories/upsert`：新增或修改一条记忆（加 `memory_id` 即修改）。
- `DELETE /api/memories/{id}` / `DELETE /api/memories`：删除一条 / 清空全部。

### 平台接口速查

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/conversations` | 创建会话 |
| GET | `/api/conversations` | 列出会话 |
| GET | `/api/conversations/{id}` | 会话详情与消息 |
| PATCH | `/api/conversations/{id}` | 重命名 / 开关长期记忆 |
| DELETE | `/api/conversations/{id}` | 删除会话 |
| POST | `/api/conversations/{id}/chat` | 非流式对话 |
| POST | `/api/conversations/{id}/chat/stream` | SSE 流式对话 |
| GET | `/api/memories` | 查看长期记忆 |
| POST | `/api/memories/upsert` | 新增/修改长期记忆 |
| DELETE | `/api/memories/{id}` | 删除一条记忆 |
| DELETE | `/api/memories` | 清空长期记忆 |
| GET | `/api/agents` | 获取已配置 Agent 能力 |
| GET | `/api/models` | 运行时模型管理：列出全部生效 Profile（不含密钥） |
| POST | `/api/models` | 运行时模型管理：新增 Profile |
| PATCH | `/api/models/{id}` | 运行时模型管理：更新 Profile（可换模型/厂家/参数） |
| DELETE | `/api/models/{id}` | 运行时模型管理：删除 Profile（恢复为配置文件定义） |

可运行的平台客户端示例见 `scripts/platform_client_example.py`。

---

## 2. 无状态 /v1/* 接入（高阶）

### 2.1 服务地址与认证

```text
Base URL: http://127.0.0.1:8000
Authorization: Bearer <SERVICE_API_KEY>
Content-Type: application/json
```

本地当前验证服务可能运行在其他端口，以启动日志为准。

仓库已提供可直接运行的两轮调用示例：[scripts/client_example.py](scripts/client_example.py)

```powershell
$env:CHAT_SERVICE_URL='http://127.0.0.1:8000'
$env:SERVICE_API_KEY='local-dev-token'
$env:CHAT_AGENT_ID='deepseek-text'
py -3.12 scripts/client_example.py
```

该脚本演示了最核心的接入动作：第一轮不传状态，成功后保存服务返回的状态，并在第二轮原样传回。

### 2.2 获取可用 Agent

客户端先获取服务端已配置的模型，不要自行填写模型名：

```http
GET /v1/agents
Authorization: Bearer local-dev-token
```

```json
{
  "agents": [
    {
      "agent_id": "deepseek-text",
      "display_name": "DeepSeek 文本模型",
      "configured": true,
      "model": "deepseek-v4-flash",
      "provider": "deepseek",
      "supports_images": false,
      "context_size": 1000000,
      "max_output_tokens": 8192
    }
  ]
}
```

前端根据 `supports_images` 控制图片入口。最终调用聊天接口时传 `agent_id`，不是 `model`。

### 2.3 发起第一轮对话

```http
POST /v1/chat
Authorization: Bearer local-dev-token
Content-Type: application/json
```

```json
{
  "request_id": "req-001",
  "tenant_id": "tenant-a",
  "user_id": "user-123",
  "conversation_id": "conversation-456",
  "agent_id": "deepseek-text",
  "message": {
    "content": [
      {"type": "text", "text": "请记住我喜欢简洁回答"}
    ]
  },
  "conversation_state": null,
  "state_version": 0,
  "memory_enabled": false,
  "user_memory_state": null
}
```

成功响应：

```json
{
  "request_id": "req-001",
  "assistant_message": "好的。",
  "conversation_state": {
    "schema_version": 1,
    "agentscope_version": "2.0.5",
    "tenant_id": "tenant-a",
    "user_id": "user-123",
    "conversation_id": "conversation-456",
    "agent_id": "deepseek-text",
    "state_version": 1,
    "agent_state": {}
  },
  "new_state_version": 1,
  "user_memory_state": null,
  "usage": {"input_tokens": 20, "output_tokens": 5},
  "summary_applied": false,
  "warnings": []
}
```

业务平台需要保存：

- 用户消息和 `assistant_message`
- 完整 `conversation_state`
- `new_state_version`
- 开启长期记忆时的完整 `user_memory_state`

`agent_state` 是 AgentScope 内部状态，必须作为不透明 JSON 原样保存，不要自行修改或裁剪。

### 2.4 发起下一轮对话

下一轮使用同一组身份字段，并传回上次成功状态：

```json
{
  "request_id": "req-002",
  "tenant_id": "tenant-a",
  "user_id": "user-123",
  "conversation_id": "conversation-456",
  "agent_id": "deepseek-text",
  "message": {
    "content": [{"type": "text", "text": "我刚才说喜欢什么风格？"}]
  },
  "conversation_state": {
    "schema_version": 1,
    "agentscope_version": "2.0.5",
    "tenant_id": "tenant-a",
    "user_id": "user-123",
    "conversation_id": "conversation-456",
    "agent_id": "deepseek-text",
    "state_version": 1,
    "agent_state": {}
  },
  "state_version": 1,
  "memory_enabled": false,
  "user_memory_state": null
}
```

每次成功后版本加 1。身份或版本不匹配会返回 HTTP 409。

### 2.5 SSE 流式调用

```http
POST /v1/chat/stream
```

请求体与 `/v1/chat` 完全相同。事件顺序：

```text
event: started
data: {"request_id":"req-003"}

event: delta
data: {"request_id":"req-003","delta":"你"}

event: delta
data: {"request_id":"req-003","delta":"好"}

event: completed
data: {"request_id":"req-003","assistant_message":"你好","conversation_state":{},"new_state_version":1,"user_memory_state":null,"usage":{"input_tokens":10,"output_tokens":2},"summary_applied":false,"warnings":[]}
```

流式失败：

```text
event: error
data: {"error":{"code":"REQUEST_TIMEOUT","message":"The model request timed out","details":{}},"request_id":"req-003"}
```

提交规则：

| 结果 | 是否保存新状态 |
|---|---:|
| 普通接口 HTTP 200 | 是 |
| SSE `completed` | 是 |
| SSE `error` | 否 |
| 网络断开或用户取消 | 否 |
| HTTP 4xx/5xx | 否 |

浏览器不能用原生 `EventSource` 发 POST，应使用 `fetch` 读取 `ReadableStream`，并使用 SSE parser 解析事件。不要把单次网络 chunk 当成完整事件。

### 2.6 图片调用

图片 URL：

```json
{
  "message": {
    "content": [
      {"type": "text", "text": "描述图片"},
      {
        "type": "image_url",
        "url": "https://cdn.example.com/image.png",
        "media_type": "image/png"
      }
    ]
  }
}
```

Base64 图片：

```json
{
  "type": "image_base64",
  "data": "iVBORw0KGgoAAAANSUhEUg...",
  "media_type": "image/png"
}
```

注意：

- Base64 不带 `data:image/png;base64,` 前缀。
- 支持 `image/jpeg`、`image/png`、`image/webp`。
- URL 必须使用 HTTPS。
- 默认最多 5 张图，单张 Base64 解码后最多 10 MiB。
- 文本模型收到图片直接返回 `MODEL_IMAGE_UNSUPPORTED`。

### 2.7 客户端持久化伪代码

```python
old_state = database.load_conversation(conversation_id)

result = call_chat_api(
    conversation_state=old_state.conversation_state,
    state_version=old_state.state_version,
)

if result.completed:
    database.atomic_commit(
        expected_state_version=old_state.state_version,
        user_message=result.user_message,
        assistant_message=result.assistant_message,
        conversation_state=result.conversation_state,
        new_state_version=result.new_state_version,
        user_memory_state=result.user_memory_state,
    )
```

同一 `conversation_id` 必须串行调用，并用 `state_version` 做数据库乐观锁。`request_id` 只用于追踪，不提供幂等能力。

客户平台的完整数据库表设计、字段说明、ER 关系与乐观锁事务实现，见 [数据库设计文档.md](数据库设计文档.md)（配套建表脚本 `schema.sql`、存取示例 `scripts/customer_store_example.py`）。

---

## 3. 模型配置

模型、厂家地址、密钥、系统提示词和采样参数全部由服务端控制。客户端只能选择 `agent_id`。

### 3.1 配置方式

启动时按以下顺序读取：

1. `MODEL_PROFILES_JSON`：内联 JSON，优先级最高。
2. `MODEL_PROFILES_FILE`：JSON 文件路径，推荐使用。
3. 两者都未设置时，使用兼容的 `DEEPSEEK_*` 和 `OPENAI_*` 环境变量。

完整模板：[config/model_profiles.example.json](config/model_profiles.example.json)

启动示例：

```powershell
$env:MODEL_PROFILES_FILE='config/model_profiles.json'
$env:DEEPSEEK_API_KEY='...'
$env:OPENAI_API_KEY='...'
py -3.12 -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

### 3.2 最小 Profile

```json
{
  "profiles": [
    {
      "enabled": true,
      "agent_id": "deepseek-text",
      "display_name": "DeepSeek 文本模型",
      "provider": "deepseek",
      "model": "deepseek-v4-flash",
      "api_key_env": "DEEPSEEK_API_KEY",
      "base_url": "https://api.deepseek.com/v1",
      "supports_images": false,
      "context_size": 1000000,
      "max_output_tokens": 8192,
      "parameters": {"temperature": 0.7}
    }
  ]
}
```

密钥不写入 JSON。`api_key_env` 填环境变量名称，服务启动时再读取实际值。

### 3.3 核心字段

| 字段 | 必填 | 作用 |
|---|---:|---|
| `enabled` | 否 | 默认 `true`；设为 `false` 时忽略该 Profile |
| `agent_id` | 是 | 对外稳定 ID，也是状态和记忆隔离的一部分 |
| `provider` | 是 | AgentScope 模型适配器类型 |
| `model` | 是 | 厂家实际模型名称 |
| `api_key_env` | 除 Ollama 外 | API Key 所在环境变量名称 |
| `base_url` | 按厂家 | 厂家 API 地址 |
| `supports_images` | 是 | 是否允许图片输入，必须符合模型真实能力 |
| `context_size` | 是 | 模型真实上下文上限 |
| `max_output_tokens` | 是 | 最大输出 token，同时传给 AgentScope Parameters |
| `system_prompt` | 否 | Agent 专属提示词，缺省使用 `CHAT_SYSTEM_PROMPT` |
| `parameters` | 否 | 厂家模型参数，如 `temperature`、`top_p`、思考参数 |
| `max_retries` | 否 | 上游重试次数，默认 3 |
| `retry_delay` | 否 | 重试间隔秒数，默认 1.0 |

高级字段：OpenAI 可用 `organization` 和 `extra_body`；Ollama 使用 `host`；xAI 使用 `api_host`；`client_kwargs` 会透传给厂家 SDK 客户端。

### 3.4 AgentScope 厂家映射

| `provider` | AgentScope 模型类 | Credential |
|---|---|---|
| `openai_chat` | `OpenAIChatModel` | `OpenAICredential` |
| `openai_response` | `OpenAIResponseModel` | `OpenAICredential` |
| `deepseek` | `DeepSeekChatModel` | `DeepSeekCredential` |
| `dashscope` | `DashScopeChatModel` | `DashScopeCredential` |
| `anthropic` | `AnthropicChatModel` | `AnthropicCredential` |
| `gemini` | `GeminiChatModel` | `GeminiCredential` |
| `moonshot` | `MoonshotChatModel` | `MoonshotCredential` |
| `ollama` | `OllamaChatModel` | `OllamaCredential` |
| `xai` | `XAIChatModel` | `XAICredential` |

其他提供 OpenAI Chat Completions 兼容接口的厂家使用 `openai_chat`：

```json
{
  "agent_id": "vendor-chat",
  "provider": "openai_chat",
  "model": "vendor-model-id",
  "api_key_env": "VENDOR_API_KEY",
  "base_url": "https://llm.vendor.example/v1",
  "supports_images": true,
  "context_size": 1000000,
  "max_output_tokens": 8192,
  "parameters": {"temperature": 0.7},
  "extra_body": {}
}
```

### 3.5 参数与启动校验

`parameters` 会交给对应 AgentScope `Parameters` 模型校验。字段写错或数值越界会导致服务启动失败。

常用参数：

- OpenAI：`temperature`、`top_p`、`reasoning_effort`、`thinking_enable`
- DeepSeek：`temperature`、`top_p`、`reasoning_effort`、`thinking_enable`
- DashScope：`temperature`、`top_p`、`top_k`、`thinking_budget`
- Anthropic：`thinking_enable`、`thinking_budget`
- Gemini：`temperature`、`top_p`、`thinking_budget`
- Moonshot/xAI：`temperature`、`top_p`、`reasoning_effort`
- Ollama：`temperature`、`thinking_enable`

不要在 `parameters` 中重复设置 `max_tokens`，服务会用 `max_output_tokens` 覆盖。

项目使用 `agentscope[models]==2.0.5` 安装厂家 SDK。必须使用项目独立虚拟环境，避免和全局 Python 包冲突。

### 3.6 运行时模型管理接口（`/api/models`）

模型默认在启动时从配置文件 / 环境变量读取。**无需改文件、无需重启**，管理员可通过下面的接口在运行时增删改查模型 Profile：新增的模型立即出现在 `GET /api/agents`、`GET /v1/agents` 和会话可选列表里，客户端照常只传 `agent_id`。

> 管理接口随平台模式提供（需要 `PLATFORM_API_ENABLED=true`），并受 `SERVICE_API_KEY` Bearer 认证保护（与其它 `/api/*` 一致）。纯无状态部署（`PLATFORM_API_ENABLED=false`）不提供这些接口。

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/models` | 列出全部生效 Profile（运行时 + 静态），不含密钥 |
| POST | `/api/models` | 新增一个运行时 Profile |
| PATCH | `/api/models/{agent_id}` | 部分更新一个已有 Profile（运行时或静态） |
| DELETE | `/api/models/{agent_id}` | 删除运行时 Profile，恢复为配置文件定义 |

规则与语义：

- **运行时覆盖静态**：同一个 `agent_id` 下，运行时 Profile 优先；删除或停用后回落静态配置。
- **立即生效**：增删改会立刻替换该 `agent_id` 使用的模型（正在处理中的请求仍用旧模型完成本轮）。
- **持久化**：运行时 Profile 写入平台 SQLite 的 `model_profiles` 表，重启后仍生效。
- **密钥不回传**：`api_key` / `api_key_env` 接受写入，但任何列表接口都不返回密钥或 `api_key` 字段。直接传的 `api_key` 会明文存在平台库，请保护 `data/platform.sqlite3`；也可以传 `api_key_env` 引用已有环境变量。
- **`enabled=false`**：表示忽略该运行时 Profile（等价于删除覆盖，回落静态）。
- **校验**：provider、`context_size` 与摘要阈值、`max_output_tokens`、`max_retries`、`parameters` 在写入时校验；非法返回 `400 MODEL_PROFILE_INVALID`，未知 provider 返回 `422`。

新增一个模型：

```http
POST /api/models
Authorization: Bearer <SERVICE_API_KEY>
Content-Type: application/json
```

```json
{
  "agent_id": "vendor-a",
  "display_name": "Vendor Chat",
  "provider": "openai_chat",
  "model": "vendor-model",
  "api_key": "sk-...",
  "base_url": "https://llm.vendor.example/v1",
  "supports_images": true,
  "context_size": 1000000,
  "max_output_tokens": 8192,
  "parameters": {"temperature": 0.7}
}
```

响应 `201`，返回该 Profile 的管理视图（`source=runtime`，且不含密钥）：

```json
{
  "model": {
    "agent_id": "vendor-a",
    "display_name": "Vendor Chat",
    "configured": true,
    "model": "vendor-model",
    "provider": "openai_chat",
    "supports_images": true,
    "context_size": 1000000,
    "max_output_tokens": 8192,
    "source": "runtime",
    "enabled": true,
    "active": true,
    "base_url": "https://llm.vendor.example/v1"
  }
}
```

换模型（把某个 agent 换成另一型号，只改想改的字段）：

```http
PATCH /api/models/deepseek-text
Authorization: Bearer <SERVICE_API_KEY>
Content-Type: application/json

{
  "model": "deepseek-v3"
}
```

恢复为配置文件定义：

```http
DELETE /api/models/deepseek-text
```

> 请求体字段与配置文件 Profile 一致（见 3.2、3.3）。`PATCH` 只更新请求中出现的字段，其余保持现有值。

---

## 4. 框架结构

### 4.1 目录职责

```text
chat_service/
├─ app/
│  ├─ main.py             FastAPI 路由、认证、超时、SSE
│  ├─ config.py           环境变量和模型 Profile 加载
│  ├─ model_registry.py   AgentScope Credential/ChatModel 构造
│  ├─ service.py          多轮对话、摘要、长期记忆编排
│  ├─ state.py            AgentState 恢复、校验和消息转换
│  ├─ memory.py           长期记忆召回、抽取和 Middleware
│  ├─ schemas.py          公共请求/响应 Schema
│  ├─ errors.py           稳定错误码
│  ├─ routes.py           平台接口 /api/*（服务端自动持久化）
│  ├─ store.py            SQLite 存储（会话、消息、长期记忆）
│  └─ ...                 其他后端代码
├─ demo_frontend/         参考页面（独立静态，仅演示）
├─ config/
│  └─ model_profiles.example.json
├─ tests/
└─ 接口文档.md
```

### 4.2 一次请求的内部流程

```text
HTTP 请求
   │
   ▼
FastAPI main.py
   │ 认证、大小限制、超时、错误转换
   ▼
ChatService
   │
   ├─ ModelRegistry：按 agent_id 获取 AgentScope ChatModel
   ├─ state.py：校验并恢复 AgentState
   ├─ memory.py：可选召回长期记忆
   ├─ service.py：超过 200k token 时摘要，保留最近 8 轮
   ├─ Agent.reply / Agent.reply_stream
   └─ 导出新 AgentState 和长期记忆状态
   │
   ▼
JSON 响应或 SSE completed
```

### 4.3 AgentScope 的使用位置

核心对象：

| AgentScope 对象 | 本项目用途 |
|---|---|
| `Agent` | 执行单次非流式或流式回复 |
| `AgentState` | 保存上下文、摘要和 session 状态 |
| `ChatModelBase` | 统一不同厂家的模型调用 |
| `Msg` / `TextBlock` | 文本消息 |
| `DataBlock` / `URLSource` / `Base64Source` | 图片输入 |
| `MiddlewareBase` | 注入召回的长期记忆 |
| `TextBlockDeltaEvent` | 转换为 SSE `delta` |
| `ModelCallEndEvent` | 汇总 token 用量 |

服务内部的主要调用形式：

```python
profile = registry.profile(request.agent_id)
model = registry.get(request.agent_id, stream=True)

agent = Agent(
    name=request.agent_id,
    system_prompt=profile.system_prompt,
    model=model,
    state=restored_agent_state,
    middlewares=middlewares,
)

async for event in agent.reply_stream(user_message, yield_final_msg=True):
    # TextBlockDeltaEvent -> SSE delta
    # 最终 Msg -> 新 AgentState -> SSE completed
    ...
```

业务开发不要绕过 `ChatService` 直接调用 `Agent`，否则会跳过身份校验、状态版本、摘要和长期记忆提交规则。

### 4.4 如何扩展

增加同一厂家的新模型：只增加 JSON Profile，不改代码。

增加新的 OpenAI-compatible 厂家：使用 `provider=openai_chat` 和厂家 `base_url`，通常不改代码。

增加 AgentScope 尚未内置的非兼容厂家：

1. 实现或引入 `ChatModelBase` 子类与 Credential。
2. 在 `MODEL_CLASSES` 注册 Provider。
3. 在 `ModelRegistry._build()` 构造 Credential 和模型。
4. 添加 Profile 参数校验和无网络构造测试。
5. 验证非流式、SSE、token 统计、结构化摘要和图片输入。

---

## 5. 接口速查

### 5.1 正式接口

> 平台 `/api/*` 接口见第 1 章；以下为无状态 `/v1/*` 高阶接口。

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/v1/agents` | 获取可用 Agent 和能力 |
| POST | `/v1/chat` | 非流式聊天 |
| POST | `/v1/chat/stream` | SSE 流式聊天 |
| POST | `/v1/memories/inspect` | 查看或初始化长期记忆状态 |
| POST | `/v1/memories/upsert` | 新增或修改记忆 |
| POST | `/v1/memories/delete` | 删除一条记忆 |
| POST | `/v1/memories/clear` | 清空记忆 |
| GET | `/health` | 进程存活检查 |
| GET | `/ready` | 模型静态配置就绪检查 |
| GET | `/docs` | Swagger UI |

### 5.2 聊天请求字段

| 字段 | 必填 | 说明 |
|---|---:|---|
| `request_id` | 是 | 每次请求唯一，最长 128 字符 |
| `tenant_id` | 是 | 租户隔离 |
| `user_id` | 是 | 用户隔离 |
| `conversation_id` | 是 | 会话隔离 |
| `agent_id` | 是 | 来自 `/v1/agents` |
| `message.content` | 是 | 至少一个文本或图片 Part |
| `conversation_state` | 后续轮必填 | 上次成功返回的完整状态 |
| `state_version` | 是 | 首轮 0，后续使用上次新版本 |
| `memory_enabled` | 否 | 默认 `false` |
| `user_memory_state` | 记忆开启时 | 按用户和 Agent 保存的完整状态 |

### 5.3 长期记忆

长期记忆由客户选择是否开启，隔离键为：

```text
tenant_id + user_id + agent_id
```

因此同一用户、同一 Agent 的不同会话可以共享记忆。客户平台负责保存完整 `user_memory_state`。

新增记忆请求：

```json
{
  "tenant_id": "tenant-a",
  "user_id": "user-123",
  "agent_id": "deepseek-text",
  "user_memory_state": null,
  "content": "用户偏好 Markdown 报告",
  "category": "preference",
  "source_conversation_id": "conversation-456"
}
```

修改时增加 `memory_id`。删除接口传当前状态和 `memory_id`。所有成功响应都返回新的完整 `user_memory_state`。

### 5.4 摘要规则

- 仅处理当前单一会话，不跨会话摘要。
- 上下文超过 200,000 token 时，在回答前生成摘要。
- 最近 8 个用户轮次及其助手回答保留原文。
- 摘要失败不返回可提交状态。
- 最近 8 轮仍超过模型安全窗口时返回 `CONTEXT_TOO_LARGE`。

### 5.5 常用错误码

| HTTP | 错误码 | 处理方式 |
|---:|---|---|
| 401 | `UNAUTHORIZED` | 检查服务 Bearer Token |
| 404 | `AGENT_NOT_FOUND` | 重新获取 `/v1/agents` |
| 409 | `STATE_VERSION_MISMATCH` | 读取数据库最新状态 |
| 409 | `STATE_IDENTITY_MISMATCH` | 检查状态是否串租户、用户或会话 |
| 409 | `MEMORY_IDENTITY_MISMATCH` | 检查记忆隔离键 |
| 413 | `REQUEST_TOO_LARGE` / `IMAGE_TOO_LARGE` | 缩小请求或图片 |
| 422 | `REQUEST_VALIDATION_ERROR` | 检查字段、类型和长度 |
| 422 | `MODEL_IMAGE_UNSUPPORTED` | 改用多模态 Agent |
| 422 | `CONTEXT_TOO_LARGE` | 减少保留内容或换大上下文模型 |
| 502 | `MODEL_REQUEST_FAILED` | 不提交状态，可有限重试 |
| 502 | `SUMMARY_FAILED` | 不提交状态，可有限重试 |
| 503 | `MODEL_NOT_CONFIGURED` | 检查 Profile 对应密钥环境变量 |
| 503 | `MODEL_PROVIDER_DEPENDENCY_MISSING` | 安装 `agentscope[models]` |
| 504 | `REQUEST_TIMEOUT` | 不提交状态，可有限重试 |

统一错误格式：

```json
{
  "error": {
    "code": "STATE_VERSION_MISMATCH",
    "message": "state_version does not match conversation_state",
    "details": {}
  },
  "request_id": "req-002"
}
```

---

## 6. 本地验证与生产边界

平台模式默认开启：后端提供 `/api/*` 和 SQLite 存储（`data/platform.sqlite3`）。参考页面位于 `demo_frontend/`，不由 FastAPI 提供。

生产部署必须：

- 如需纯无状态 `/v1/*` 部署，设置 `PLATFORM_API_ENABLED=false`。
- 平台模式下存储与乐观锁由服务端自动处理；无状态 `/v1/*` 接入时由客户自行实现。
- 配置 `SERVICE_API_KEY`。
- 使用独立虚拟环境安装依赖。
- 关闭反向代理的 SSE 缓冲。
- 不记录 API Key、Base64 图片、完整敏感消息或完整状态。

主要部署变量：

| 变量 | 默认值 |
|---|---:|
| `SUMMARY_TRIGGER_TOKENS` | `200000` |
| `SUMMARY_KEEP_ROUNDS` | `8` |
| `REQUEST_TIMEOUT_SECONDS` | `120` |
| `MAX_REQUEST_BYTES` | `62914560` |
| `MAX_IMAGES` | `5` |
| `MAX_IMAGE_BYTES` | `10485760` |
| `MAX_MEMORY_ITEMS` | `200` |
| `MEMORY_RECALL_LIMIT` | `10` |

健康检查只说明进程存活；`/ready` 只说明至少一个 Profile 静态配置完整，不会实际调用厂家验证余额、网络或模型名称。
