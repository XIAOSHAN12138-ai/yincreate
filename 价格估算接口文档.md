# 价格估算接口文档

## 接口概述

**接口名称：** 估算生成价格

**接口路径：** `/api/v1/estimate-price`

**请求方法：** POST

**接口说明：** 在用户点击生成按钮前，根据模型和生成参数实时计算预估价格

**鉴权要求：** 无需鉴权（公开接口）

---

## 请求参数

### 请求头 (Headers)

```http
Content-Type: application/json
```

### 请求体 (Body)

请求格式与生成接口完全相同，但不会真正执行生成。

```json
{
  "model": "string",              // 必填：模型名称（显示名称或 model_id）
  "output_type": "string",        // 必填：输出类型 "image" | "video" | "digital_human"
  "feature": "string",            // 可选：功能特性
  "prompt": "string",             // 可选：提示词（不影响价格）
  "parameters": {                 // 必填：生成参数
    "resolution": "string",       // 可选：分辨率 "720P" | "1080P" | "2K" | "4K"
    "duration": number,           // 可选：视频时长（秒），仅视频需要
    "with_audio": boolean,        // 可选：是否包含音频
    "ratio": "string",            // 可选：宽高比（不影响价格）
    "count": number               // 可选：生成数量（图片）
  },
  "input_files": [                // 可选：输入文件（可能影响价格）
    {
      "type": "string",           // "image" | "video" | "audio"
      "url": "string",            // 文件 URL
      "purpose": "string"         // 用途说明
    }
  ]
}
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| model | string | 是 | 模型名称或 ID | "Kling 3.0", "kling_3_0" |
| output_type | string | 是 | 输出类型 | "image", "video" |
| feature | string | 否 | 功能特性 | "text_to_video" |
| prompt | string | 否 | 提示词（不影响价格） | "一只可爱的猫咪" |
| parameters | object | 是 | 生成参数 | - |
| parameters.resolution | string | 否 | 分辨率 | "1080P", "2K", "4K" |
| parameters.duration | number | 视频必填 | 时长（秒） | 5, 10 |
| parameters.with_audio | boolean | 否 | 是否有音频 | true, false |
| parameters.ratio | string | 否 | 宽高比 | "16:9", "9:16" |
| parameters.count | number | 否 | 图片数量 | 1, 3 |
| input_files | array | 否 | 输入文件列表 | - |

---

## 响应格式

### 成功响应 (200 OK)

```json
{
  "code": 200,
  "message": "价格估算成功",
  "data": {
    "estimated_cost": 2.50,
    "currency": "CNY",
    "breakdown": {
      "base_price": "0.50元/秒",
      "duration": "5秒",
      "resolution": "1080P",
      "with_audio": "否",
      "has_input": "否",
      "total": "2.50元"
    },
    "note": "实际扣费以生成结果为准"
  }
}
```

### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | number | 状态码，200 表示成功 |
| message | string | 响应消息 |
| data | object | 价格数据 |
| data.estimated_cost | number | 预估价格（元） |
| data.currency | string | 货币单位，固定为 "CNY" |
| data.breakdown | object | 价格明细 |
| data.breakdown.base_price | string | 单价说明 |
| data.breakdown.duration | string | 时长说明（视频） |
| data.breakdown.resolution | string | 分辨率 |
| data.breakdown.with_audio | string | 是否有音频 |
| data.breakdown.has_input | string | 是否有输入文件 |
| data.breakdown.total | string | 总价格 |
| data.note | string | 提示信息 |

### 错误响应 (500 Internal Server Error)

```json
{
  "code": 500,
  "message": "价格估算失败: 模型 'Unknown Model' 不在 ai_models 表中",
  "data": null
}
```

---

## 请求示例

### 示例 1: 估算视频生成价格

**请求：**

```bash
curl -X POST http://localhost:8000/api/v1/estimate-price \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Kling 3.0",
    "output_type": "video",
    "parameters": {
      "resolution": "1080P",
      "duration": 5,
      "with_audio": false
    }
  }'
```

**响应：**

```json
{
  "code": 200,
  "message": "价格估算成功",
  "data": {
    "estimated_cost": 2.50,
    "currency": "CNY",
    "breakdown": {
      "base_price": "0.50元/秒",
      "duration": "5秒",
      "resolution": "1080P",
      "with_audio": "否",
      "has_input": "否",
      "total": "2.50元"
    },
    "note": "实际扣费以生成结果为准"
  }
}
```

### 示例 2: 估算高分辨率视频价格

**请求：**

```json
{
  "model": "Kling 3.0",
  "output_type": "video",
  "parameters": {
    "resolution": "4K",
    "duration": 10,
    "with_audio": true
  }
}
```

**响应：**

```json
{
  "code": 200,
  "message": "价格估算成功",
  "data": {
    "estimated_cost": 24.00,
    "currency": "CNY",
    "breakdown": {
      "base_price": "2.40元/秒",
      "duration": "10秒",
      "resolution": "4K",
      "with_audio": "是",
      "has_input": "否",
      "total": "24.00元"
    },
    "note": "实际扣费以生成结果为准"
  }
}
```

### 示例 3: 估算图片生成价格

**请求：**

```json
{
  "model": "Kling 3.0",
  "output_type": "image",
  "parameters": {
    "resolution": "2K",
    "count": 3
  }
}
```

**响应：**

```json
{
  "code": 200,
  "message": "价格估算成功",
  "data": {
    "estimated_cost": 0.15,
    "currency": "CNY",
    "breakdown": {
      "base_price": "0.15元/张",
      "resolution": "2K",
      "has_input": "否",
      "total": "0.15元"
    },
    "note": "实际扣费以生成结果为准"
  }
}
```

### 示例 4: 使用 model_id

**请求：**

```json
{
  "model": "kling_3_0_2k",
  "output_type": "video",
  "parameters": {
    "duration": 8
  }
}
```

**响应：**

```json
{
  "code": 200,
  "message": "价格估算成功",
  "data": {
    "estimated_cost": 9.60,
    "currency": "CNY",
    "breakdown": {
      "base_price": "1.20元/秒",
      "duration": "8秒",
      "resolution": "默认",
      "with_audio": "否",
      "has_input": "否",
      "total": "9.60元"
    },
    "note": "实际扣费以生成结果为准"
  }
}
```

---

## 价格计算逻辑

### 视频价格计算

```
总价格 = 单价(元/秒) × 时长(秒) × 价格乘数
```

**分辨率单价表：**

| 分辨率 | 单价（元/秒） |
|--------|---------------|
| 480P   | ¥0.20        |
| 720P   | ¥0.30        |
| 768P   | ¥0.35        |
| 1080P  | ¥0.50        |
| 2K     | ¥1.20        |
| 4K     | ¥2.40        |
| 默认   | ¥0.50        |

### 图片价格计算

```
总价格 = 单价(元/张) × 价格乘数
```

**分辨率单价表：**

| 分辨率 | 单价（元/张） |
|--------|---------------|
| 1080P  | ¥0.08        |
| 2K     | ¥0.15        |
| 4K     | ¥0.30        |
| 默认   | ¥0.10        |

### 特殊说明

1. **价格乘数（price_multiplier）**：
   - 默认为 1.00
   - 可在数据库中针对特定模型调整
   - 用于批量调价或特殊优惠

2. **是否有音频（with_audio）**：
   - 当前版本不影响价格
   - 预留扩展功能，未来可能对有音频视频加价

3. **输入文件（input_files）**：
   - 当前版本不影响价格
   - 预留扩展功能，未来可能对图生视频等功能差异化定价

---

## 错误码说明

| HTTP 状态码 | code | message | 说明 |
|------------|------|---------|------|
| 200 | 200 | 价格估算成功 | 估算成功 |
| 500 | 500 | 价格估算失败: ... | 定价逻辑错误（模型不存在等） |
| 500 | 500 | 请求处理失败: ... | 请求参数错误或服务器错误 |

---

## 前端集成示例

### React 示例

```jsx
import React, { useState, useEffect } from 'react';

function PriceEstimate({ model, outputType, parameters }) {
  const [price, setPrice] = useState(null);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    const estimatePrice = async () => {
      setLoading(true);
      try {
        const response = await fetch('/api/v1/estimate-price', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            model,
            output_type: outputType,
            parameters
          })
        });
        
        const result = await response.json();
        
        if (result.code === 200) {
          setPrice(result.data);
        } else {
          console.error('价格估算失败:', result.message);
        }
      } catch (error) {
        console.error('请求失败:', error);
      } finally {
        setLoading(false);
      }
    };

    if (model && outputType && parameters) {
      estimatePrice();
    }
  }, [model, outputType, parameters]);

  if (loading) return <div>计算中...</div>;
  if (!price) return null;

  return (
    <div className="price-estimate">
      <div className="total-price">
        预估价格: ¥{price.estimated_cost.toFixed(2)}
      </div>
      <div className="breakdown">
        <small>{price.breakdown.base_price} × {price.breakdown.duration}</small>
      </div>
      <div className="note">
        <small>{price.note}</small>
      </div>
    </div>
  );
}

export default PriceEstimate;
```

### Vue 3 示例

```vue
<template>
  <div v-if="price" class="price-estimate">
    <div class="total-price">
      预估价格: ¥{{ price.estimated_cost.toFixed(2) }}
    </div>
    <div class="breakdown">
      <small>{{ price.breakdown.base_price }} × {{ price.breakdown.duration }}</small>
    </div>
    <div class="note">
      <small>{{ price.note }}</small>
    </div>
  </div>
  <div v-else-if="loading">计算中...</div>
</template>

<script setup>
import { ref, watch } from 'vue';

const props = defineProps({
  model: String,
  outputType: String,
  parameters: Object
});

const price = ref(null);
const loading = ref(false);

const estimatePrice = async () => {
  loading.value = true;
  try {
    const response = await fetch('/api/v1/estimate-price', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model: props.model,
        output_type: props.outputType,
        parameters: props.parameters
      })
    });
    
    const result = await response.json();
    
    if (result.code === 200) {
      price.value = result.data;
    }
  } catch (error) {
    console.error('价格估算失败:', error);
  } finally {
    loading.value = false;
  }
};

watch(() => [props.model, props.outputType, props.parameters], estimatePrice, {
  deep: true,
  immediate: true
});
</script>
```

---

## 性能优化建议

### 1. 防抖处理

用户调整参数时，避免频繁请求：

```javascript
import { debounce } from 'lodash';

const estimatePrice = debounce(async () => {
  // 估算逻辑
}, 500); // 500ms 防抖
```

### 2. 请求缓存

相同参数的请求可以缓存结果：

```javascript
const priceCache = new Map();

async function estimatePrice(params) {
  const cacheKey = JSON.stringify(params);
  
  if (priceCache.has(cacheKey)) {
    return priceCache.get(cacheKey);
  }
  
  const result = await fetch('/api/v1/estimate-price', {
    method: 'POST',
    body: JSON.stringify(params)
  }).then(r => r.json());
  
  priceCache.set(cacheKey, result);
  return result;
}
```

### 3. 加载状态

显示加载状态提升用户体验：

```jsx
{loading && <Spinner />}
{price && <PriceDisplay price={price} />}
```

---

## 注意事项

1. **实际扣费以生成结果为准**
   - 此接口仅提供价格估算
   - 实际扣费可能因生成失败、参数调整等原因不同

2. **模型名称格式**
   - 支持显示名称（如 "Kling 3.0"）
   - 支持 model_id（如 "kling_3_0"）
   - 支持带分辨率的 model_id（如 "kling_3_0_1080p"）

3. **分辨率参数**
   - 不传 resolution 时使用默认价格
   - 传入 resolution 时使用对应分辨率价格

4. **价格调整**
   - 价格可能随时调整
   - 建议显示 "预估" 或 "参考" 字样

---

## 测试用例

### cURL 测试脚本

```bash
#!/bin/bash

BASE_URL="http://localhost:8000"

echo "测试 1: 视频生成价格估算"
curl -X POST "${BASE_URL}/api/v1/estimate-price" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Kling 3.0",
    "output_type": "video",
    "parameters": {
      "resolution": "1080P",
      "duration": 5
    }
  }'

echo -e "\n\n测试 2: 4K 视频价格估算"
curl -X POST "${BASE_URL}/api/v1/estimate-price" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Kling 3.0",
    "output_type": "video",
    "parameters": {
      "resolution": "4K",
      "duration": 10
    }
  }'

echo -e "\n\n测试 3: 图片价格估算"
curl -X POST "${BASE_URL}/api/v1/estimate-price" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Kling 3.0",
    "output_type": "image",
    "parameters": {
      "resolution": "2K"
    }
  }'
```

---

## 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0.0 | 2026-06-17 | 初始版本，支持基础价格估算 |

---

## 联系方式

如有问题或建议，请联系技术支持团队。
